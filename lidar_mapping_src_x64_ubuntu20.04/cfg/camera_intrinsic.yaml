%YAML:1.0

intrinsic:
  camera.fx: 911.5562
  camera.fy: 911.5304
  camera.cx: 639.9363
  camera.cy: 469.6924


  # // 六目相机：ID=0x01号相机内参 (前面的右边的相机)
  # // info.fx = 915.5451;
  # // info.fy = 915.6571;
  # // info.cx = 644.3588;
  # // info.cy = 464.9629;
  #
  # // 六目相机：ID=0x02号相机内参 (前面的左边的相机)
  # // info.fx = 912.442729;
  # // info.fy = 912.521239;
  # // info.cx = 647.046823;
  # // info.cy = 488.954874;
  #
  # // 六目相机：ID=0x03号相机内参 (右侧相机)
  # // info.fx = 911.9081;
  # // info.fy = 912.0118;
  # // info.cx = 647.7556;
  # // info.cy = 471.3920;
  #
  # // 六目相机：ID=0x04号相机内参 (左侧相机)
  # // info.fx = 911.5562;
  # // info.fy = 911.5304;
  # // info.cx = 639.9363;
  # // info.cy = 469.6924;
  #
  # // 六目相机：ID=0x05号相机内参 (后面相机)
  # // info.fx = 918.3346;
  # // info.fy = 918.2145;
  # // info.cx = 635.1340;
  # // info.cy = 467.1713;
  #
  # ////////////////////////////////////////////////
  # // 降采样后标定参数：降采样后的图像尺寸是 640*480
  # ////////////////////////////////////////////////
  # // 六目相机：ID=0x01号相机内参 (前面的右边的相机)
  # // info.fx = ;
  # // info.fy = ;
  # // info.cx = ;
  # // info.cy = ;
  #
  # // 六目相机：ID=0x02号相机内参 (前面的左边的相机)
  # // info.fx = ;
  # // info.fy = ;
  # // info.cx = ;
  # // info.cy = ;
  #
  # // 六目相机：ID=0x03号相机内参 (右侧相机)
  # // info.fx = 448.2737;
  # // info.fy = 449.7472;
  # // info.cx = 326.7019;
  # // info.cy = 243.8449;
  #
  # // 六目相机：ID=0x04号相机内参 (左侧相机)
  # // info.fx = 451.5662;
  # // info.fy = 452.5121;
  # // info.cx = 323.8785;
  # // info.cy = 230.4068;
  #
  # // 六目相机：ID=0x05号相机内参 (后面相机)
  # // info.fx = ;
  # // info.fy = ;
  # // info.cx = ;
  # // info.cy = ;
